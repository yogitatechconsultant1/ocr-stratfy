{
    "type": "object",
    "properties": {
        "securityTokens": {
            "title": "${strings.login.loginCredentials.title}",
            "description": "${strings.login.loginCredentials.description}",
            "type": "array",
            "items": {
                "type": "object",
                "required": [
                    "name",
                    "key"
                ],
                "properties": {
                    "name": {
                        "type": "string",
                        "title": "${strings.login.loginFormFields.username.title}",
                        "description": ""
                    },
                    "key": {
                        "type": "string",
                        "title": "${strings.login.loginFormFields.password.title}",
                        "description": ""
                    }
                }
            }
        }
    }
}
